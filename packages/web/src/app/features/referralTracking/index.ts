import { ReferralTrackingContentWrapper } from '@features/referralTracking/view/ReferralTrackingContentWrapper'
import { ReferralTrackingGraphic } from '@features/referralTracking/view/ReferralTrackingGraphic'
import { ReferralTrackingHeader } from '@features/referralTracking/view/ReferralTrackingHeader'
import { ReferralTrackingInfo } from '@features/referralTracking/view/ReferralTrackingInfo'
import { ReferralTrackingRoot } from '@features/referralTracking/view/ReferralTrackingRoot'

export const ReferralTracking = {
  root: ReferralTrackingRoot,
  header: ReferralTrackingHeader,
  contentWrapper: ReferralTrackingContentWrapper,
  info: ReferralTrackingInfo,
  graphic: ReferralTrackingGraphic,
}
