import { tv } from 'tailwind-variants'

export const myOwnBooksRootTV = tv({
  base: 'size-full relative'
})

export const myOwnBooksContentTV = tv({
  base: 'm-auto 2xl:w-[1350px] min-h-[500px] overflow-auto scrollbar-thin'
})
