import { tv } from 'tailwind-variants'

export const dragScrollTV = tv({
  base: 'max-w-[1600px] cursor-grab px-4 flex py-2 space-x-3 overflow-x-scroll scrollbar-hide duration-300 relative lg:-left-9'
})
