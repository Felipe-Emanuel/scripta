import { tv } from 'tailwind-variants'

export const buttonRootTv = tv({
  base: 'px-2 py-4 h-fit w-full flex items-center justify-center m-auto rounded-xl duration-500 hover:bg-primary/75 cursor-pointer bg-primary border-0',
})
